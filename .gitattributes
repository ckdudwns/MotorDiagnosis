# Auto detect text files and perform LF normalization
* text=auto

# Model checksums pin exact bytes on Windows and Linux; never normalize endings.
models/pump-event-verifier/*.json -text
models/pump-forecast/model.json -text
